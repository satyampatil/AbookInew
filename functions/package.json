{
  "name": "abooki-functions",
  "private": true,
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions,hosting,firestore:rules",
    "serve": "firebase emulators:start --only functions,hosting,firestore"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.2"
  }
}
